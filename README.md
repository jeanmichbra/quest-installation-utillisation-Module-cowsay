# quest-installation-utillisation-Module-cowsay

Installation and use of the cowsay module. Changing a sentence.

```
var cowsay = require("cowsay");
console.log(cowsay.say({
	text : "hello boy",
	e : "oO",
	T : "U "
}));
```
